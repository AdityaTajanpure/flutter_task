class User {
  String name;
  int amountWon;
  int timeAgo;
  // User may have extra properties but for the sake of this demo I'll only be using these properties

  User({required this.name, required this.amountWon, required this.timeAgo});
}
